<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TourClick" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorPrimaryVariant">@color/green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/green_light</item>
        <item name="colorSecondaryVariant">@color/green_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Цвета для темной темы -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>

        <!-- Текстовые цвета -->
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_dark</item>
        <item name="android:textColorHint">@color/text_hint_dark</item>

        <!-- Настройки навигационной панели с проверкой версии -->
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorSurface</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>

        <!-- Для совместимости со старыми версиями -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Только для основной кнопки -->
        <item name="materialButtonStyle">@style/Widget.TourClick.PrimaryButton</item>

        <!-- Material компоненты -->
        <item name="materialThemeOverlay">@style/ThemeOverlay.TourClick.Dark</item>
    </style>

    <style name="Theme.TourClick" parent="Base.Theme.TourClick" />
</resources>